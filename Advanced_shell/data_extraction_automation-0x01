#!/bin/bash

# Task 1: Extract Pokémon Data (using jq, awk, sed)

JSON_FILE="data.json"

# Extract fields from JSON
name=$(jq -r '.name' "$JSON_FILE" | sed 's/.*/\u&/')
height=$(jq -r '.height' "$JSON_FILE")
weight=$(jq -r '.weight' "$JSON_FILE")
type=$(jq -r '.types[0].type.name' "$JSON_FILE" | sed 's/.*/\u&/')

# Convert raw units to meters and kg (Pokémon API units: height=decimeters, weight=hectograms)
height_m=$(awk -v h="$height" 'BEGIN { printf "%.1f", h/10 }')
weight_kg=$(awk -v w="$weight" 'BEGIN { printf "%.0f", w/10 }')

# Final formatted output
echo "$name is of type $type, weighs ${weight_kg}kg, and is ${height_m}m tall."


chmod +x parse_pikachu
./parse_pikachu

Pikachu is of type Electric, weighs 6kg, and is 0.4m tall.



